source "http://rubygems.org"

gemspec

# Load local gems according to developer preference.
if File.exist?(File.expand_path('../.gemfile', __FILE__))
  eval(File.read(File.expand_path('../.gemfile', __FILE__)))
end
